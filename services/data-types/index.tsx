export interface ListSidebarProps {
  label: string;
  link: string;
  active?: boolean;
};

export interface CardBenefitProps {
  title?: string;
  desc?: string;
  icon: string;
}

export interface UserTypes {
  id: string,
}